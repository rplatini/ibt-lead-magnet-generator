# Anthropic API key used by both Agent A (template builder) and Agent B
# (lead-magnet filler). Required for any agent run.
ANTHROPIC_API_KEY=sk-ant-...

# Port for the Express dev server. Defaults to 7777 if unset.
LMG_WEB_PORT=7777
